# 项目收录与贡献指南

感谢向 `HQU-AIot/2026_NUEDC` 提交开源项目。

## 推荐工作方式

每个队伍或成员在 `HQU-AIot` 组织内创建一个独立仓库。不要把多个队伍的完整工程直接混放到本索引仓库，以免权限、历史和依赖互相干扰。

组织当前采用基础读取权限：项目仓库创建者管理自己的仓库，其他普通成员默认不能修改该仓库。需要协作时，由仓库管理员单独邀请协作者或为团队授权。

## 收录条件

提交申请前，请确认：

- 仓库位于 `HQU-AIot` 组织下，并已设为公开。
- `README.md` 写明题号、项目简介、成员或负责人、软硬件环境和复现步骤。
- 仓库包含明确的开源许可证。
- 第三方代码、模型、图片、数据和硬件资料注明来源与许可证。
- 已删除 token、密钥、账号、个人隐私和不应公开的竞赛材料。
- 大体积二进制、构建产物和开发工具缓存已通过 `.gitignore` 排除；必要时使用 Git LFS。

## 提交收录申请

1. 打开本仓库的 Issues。
2. 选择“项目收录申请”。
3. 完整填写题号、项目名称、仓库地址、负责人、技术方向、许可证和项目状态。
4. 等待维护者核验并加入索引。

## 修改索引

普通成员可通过 Pull Request 建议修改，但不要直接改写其他项目的信息。对现有项目条目的重大修改，应由项目负责人确认。

## 项目状态

- `开发中`：仍在持续开发或整理。
- `可复现`：已提供足够的构建、烧录或运行说明。
- `已归档`：不再维护，但保留资料供参考。

## 许可证提示

每个独立项目自行选择许可证，并确保与所用依赖兼容：

- MIT：条款简洁、限制较少。
- Apache-2.0：包含明确的专利授权条款。
- GPL-3.0：要求衍生发布保持相同的开源许可。

如无法确认第三方材料是否允许开源，请先不要上传，并向维护者说明。
